name: Simple CI

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: debian

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run a simple script
        run: echo "🎉 Hello from GitHub Actions!"